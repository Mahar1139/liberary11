kind = "web"
previewPath = "/"
title = "School Library Management System"
version = "1.0.0"
id = "artifacts/school-library"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25601

[services.development]
run = "pnpm --filter @workspace/school-library run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/school-library", "run", "build" ]
publicDir = "artifacts/school-library/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25601"
BASE_PATH = "/"
